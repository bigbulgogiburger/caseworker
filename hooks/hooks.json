{
  "description": "caseworker: git commit / git push are judged against the branch state JSON (gate + review records). Both shell tools (Bash, PowerShell) are matched — matching only one leaves the other as an unguarded path. This is the only hook.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/commit-gate.mjs\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
